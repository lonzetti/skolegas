#include <iostream>
#include <stdlib.h>
#include <string>
#include <restclient-cpp/restclient.h>

using namespace std;


class doorControl{
public:
	doorControl();
    void open();
    void close();
};